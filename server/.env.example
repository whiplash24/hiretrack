# Server env — copy to .env locally, set real values in Render dashboard for prod
PORT=5000

# MongoDB Atlas connection string
MONGO_URI=

# JWT signing secret (long random string)
JWT_SECRET=

# Origin(s) allowed by CORS in production — comma-separated for multiple
# Leave unset in local dev to allow all origins
CLIENT_URL=
